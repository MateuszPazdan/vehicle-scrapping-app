apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vehicle-ingress
spec:
  rules:
    - host: vehicle-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 80
